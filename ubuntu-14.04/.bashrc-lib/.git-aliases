# GIT ALIASES
# @repo github.com/mehmetsoylucom/vim-tmux

alias gg='sudo git gui'
alias gcm='git checkout master'
alias gpom='git pull origin master'
alias gb='git branch'
alias gr='git fetch --all && git reset --hard && git clean -f'

